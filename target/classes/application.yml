spring:
  application:
    name: linkpeer-admin-cli
  config:
    import: optional:file:.env[.properties], optional:file:${user.home}/.linkpeer/.env[.properties]
  datasource:
    url: ${SUPABASE_DB_URL:jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?sslmode=require}
    username: ${SUPABASE_DB_USERNAME:postgres.eodpcmtoqblwnyutuuhg}
    password: ${SUPABASE_DB_PASSWORD:Swayanshu@119Shibu}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  main:
    web-application-type: none

logging:
  level:
    root: INFO
    org.springframework: WARN
    org.hibernate: WARN
    com.linkpeer.admin: INFO
